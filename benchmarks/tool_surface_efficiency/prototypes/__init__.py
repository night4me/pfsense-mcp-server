"""Benchmark-only prototype designs for POST-v1.0 PRIORITY #2 (tool-surface
efficiency). Nothing here is imported by, or reachable from, the production
runtime (`src/pfsense_mcp`) -- these modules exist only to be measured by
the sibling benchmark scripts and are never registered as MCP tools."""
